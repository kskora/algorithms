package pl.kskora.algorithms.dac;

import pl.kskora.algorithms.sort.MergeSort;

/**
 * Class counting inversions in nlog(n) time.
 * Implementation is based on divide and conquer approach
 * similar as in @{@link MergeSort} algorithm.
 */
public class InversionsCounter {

    /**
     * Calculate number of inversions in given array.
     * Inversion is defined when we have indexes i and j
     * that i < j and array values meet condition: A[i] > A[j].
     *
     * @param array array to be checked
     * @return number of inversion in given array
     */
    public int count(int[] array) {

        return 0;
    }
}
